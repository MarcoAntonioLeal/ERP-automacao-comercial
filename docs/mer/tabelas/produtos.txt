CREATE TABLE produtos (
	id INTEGER PRIMARY KEY,
   	codigo TEXT UNIQUE NOT NULL,
   	nome TEXT NOT NULL CHECK(length(nome) <= 55),
   	cod_barras TEXT UNIQUE CHECK(length(cod_barras) BETWEEN 8 AND 14),
   	estoque REAL NOT NULL DEFAULT 0,
   	custo INTEGER NOT NULL DEFAULT 0,
   	valor_venda INTEGER NOT NULL CHECK(valor_venda > 0),
   	ativo integer not null default 1 CHECK(ativo IN (0, 1)),
   	grupo_id integer, 
	fornecedor integer,
	unidade_venda text not null default 'UN' CHECK(unidade_venda IN ('UN','KG','ML','PC','M')),
	observacao text CHECK(length(observacao) <= 100),

	/*tabelas abaixo de metadatas*/

	data_criacao TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
	criado_por INTEGER NOT NULL,
	data_atualizacao TEXT, 
	atualizado_por INTEGER,

	FOREIGN KEY(fornecedor)
	REFERENCES(fornecedores(id)),

   	FOREIGN KEY(grupo_id)
   	REFERENCES grupos(id),

	FOREIGN KEY(criado_por)
	REFERENCES usuarios(usuario_id),

	FOREIGN KEY(atualizado_por)
	REFERENCES usuarios(usuario_id)
)

Observações para tabela de produtos
- A colouna codigo de produtos será P0001, com incremento de P0002 e assim por diante
- Todos os campos precisam do tratamento de trim para espaçamento 
- Todos os campos precisam de tratamento para uppercase
- Os campos de check length tem de ser ajustados para o número de caracteres correspondentes aos do campo de input no front
- Os campos de valor serão inseridos como float e "," no front, no back sendo tratado como inteiro "valor * 100" e para o BD convertido em float novamente porém colocando "." no lugar da ","
- O campo de unidade de venda, verificar no select do front quais valores temos